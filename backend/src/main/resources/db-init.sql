-- ShopiNow Database Initialization Script
-- Run this script to create the database (if not using Hibernate auto-create)

-- Create database (run this in PostgreSQL command line, not here)
-- CREATE DATABASE shopinow;

-- Connect to shopinow database first, then run the following:

-- Note: Tables will be created automatically by Hibernate on first run
-- This script is for reference or manual setup if needed

-- Categories (will be populated by DataSeeder)
-- Products (will be populated by DataSeeder)
-- Users (created via registration)
-- Cart Items (created via API)
-- Orders (created via API)


